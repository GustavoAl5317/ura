{
  "name": "ura-ai",
  "version": "1.0.0",
  "description": "URA com IA para atendimento automático - Aquitelecom",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "assistant": "ts-node --transpile-only src/assistant/index.ts",
    "assistant:start": "node dist/assistant/index.js",
    "test:veredito": "ts-node --transpile-only test-veredito.ts",
    "test:espelho": "ts-node --transpile-only test-sgp-index.ts",
    "sync:espelho": "ts-node --transpile-only sync-espelho.ts",
    "test:voz": "ts-node --transpile-only test-voz.ts",
    "test:zabbix": "ts-node --transpile-only test-zabbix-falha.ts",
    "test:admin": "ts-node --transpile-only test-admin.ts",
    "test:migracao": "ts-node --transpile-only test-migracao.ts",
    "test:resumo": "ts-node --transpile-only test-resumo.ts",
    "test:sla": "ts-node --transpile-only test-sla-chat.ts",
    "test:sla-evolution": "ts-node --transpile-only test-sla.ts",
    "test:netflow": "ts-node --transpile-only test-netflow.ts",
    "test:relatorios": "ts-node --transpile-only test-relatorios.ts",
    "test:equipes": "ts-node --transpile-only test-equipes.ts",
    "test:acesso": "ts-node --transpile-only test-acesso.ts",
    "test:publico": "ts-node --transpile-only test-publico.ts",
    "test:ctos": "ts-node --transpile-only test-ctos.ts"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "better-sqlite3": "^11.10.0",
    "dotenv": "^16.4.0",
    "ffmpeg-static": "^5.3.0",
    "winston": "^3.13.0",
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20 <24"
  }
}
